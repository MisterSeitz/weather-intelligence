{
    "title": "Weather Intelligence Actor Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "batchSize": {
            "title": "Batch Size",
            "type": "integer",
            "description": "Number of concurrent API requests per batch.",
            "default": 50,
            "minimum": 1,
            "maximum": 100
        },
        "delayBetweenBatchesMs": {
            "title": "Delay Between Batches (ms)",
            "type": "integer",
            "description": "Milliseconds to wait between batches to avoid rate limiting.",
            "default": 200,
            "minimum": 0
        },
        "maxWardsToProcess": {
            "title": "Max Wards to Process",
            "type": "integer",
            "description": "Limit the number of wards to process. 0 means all wards.",
            "default": 0,
            "minimum": 0
        },
        "runTestMode": {
            "title": "Test Mode",
            "type": "boolean",
            "description": "When enabled, only processes 5 wards for testing.",
            "default": false
        }
    },
    "required": []
}